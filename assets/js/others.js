/*
PEDIR AL USUARIO UN STRING
CONTAR LAS VOCALES Y LAS CONSONANTES
IMPRIMIR EN CONSOLA
2 VOCALES Y 2 COSONANTES
*/
/*
let wordUser = prompt("Ingresa tu palabra favorita").toLowerCase
let vowels = 0
for(i = 0; i < wordUser.length; i++)
    {
        if((wordUser.charAt(i) == 'a')||
        (wordUser.charAt(i) == 'á') ||
        (wordUser.charAt(i) == 'e') ||
        (wordUser.charAt(i) == 'i') ||
        (wordUser.charAt(i) == 'í') ||
        (wordUser.charAt(i) == 'o') ||
        (wordUser.charAt(i) == 'ó') ||
        (wordUser.charAt(i) == 'u') ) {
            vowels++;
        }
    }
    console.log("vocales", vowels)
*/
/*
let wordUser = prompt("Ingresa las palabras que te gusten \n con espacio al inicio y al final").toLowerCase().trim()
let vowels = 0
let space = 0
for(i = 0; i < wordUser.length; i++){
        if((wordUser.charAt (i) == 'a')||
        (wordUser.charAt (i) == 'á')||
        (wordUser.charAt (i) == 'e')||
        (wordUser.charAt (i) == 'é')){
            vowels++;
        }else if (wordUser.charAt (i) == ' '){
            space++;
        }
    } 
    console.log("vocales", vowels, "espacios", space)
*/
/*
const numberUser = parseInt(prompt("Ingresa un valor de uno al 100"))

    for(i = 0; i <= numberUser ; i++){
     alert(i)
    }
*/
/*
let wordPal = prompt("Ingresa una palabra")
let i = wordPal.length
    for(i = -1; i >= 0 ; i--){
        console.log(i)
    }
*/